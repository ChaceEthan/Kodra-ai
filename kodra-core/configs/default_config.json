{
  "model": {
    "context_length": 256,
    "embedding_dim": 256,
    "attention_heads": 4,
    "transformer_layers": 6,
    "dropout": 0.1,
    "vocab_size": 96
  },
  "training": {
    "batch_size": 16,
    "learning_rate": 0.0003,
    "max_epochs": 20,
    "weight_decay": 0.01,
    "warmup_steps": 100,
    "seed": 42
  }
}
